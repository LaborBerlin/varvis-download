# CLAUDE.md

@AGENTS.md

## Claude Code Notes

- This file intentionally stays minimal. Keep durable project facts in `AGENTS.md`.
- Use `/memory` to inspect which project instructions Claude has loaded.
- Use available Claude Code Skills, including Superpowers Skills, when they match the task. Start sessions by loading `superpowers:using-superpowers`.
- Persistent user/feedback/project memory lives under `.claude/projects/.../memory/` on this machine; do not overwrite without reading first.
